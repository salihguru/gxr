/**
 * GXR - Go x React SSR Framework
 * 
 * This is the main entry point for the TypeScript package.
 * It exports the build tools and runtime utilities.
 */

export { build, BuildOptions } from "./cli/build";
export { watch } from "./cli/watch";
