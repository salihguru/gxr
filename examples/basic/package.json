{
  "name": "gxr-example-basic",
  "version": "0.1.0",
  "private": true,
  "description": "Basic GXR example application",
  "scripts": {
    "build": "npx --package=../../packages/gxr gxr build",
    "watch": "npx --package=../../packages/gxr gxr watch",
    "start": "npm run build && go run ."
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.7"
  }
}
